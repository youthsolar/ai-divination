# 開發工作流程規範

## 函數開發流程

### 1. 函數建立標準
- 每個 `.deluge` 檔案只包含一個函數
- 函數名稱使用命名空間格式：`Namespace.FunctionName`
- 提供詳細的繁體中文註解說明

### 2. 函數同步提醒
**重要**: 每次成功建立函數後，必須提醒用戶同步到 Zoho Creator 編輯器，因為資料庫函數不會自動同步。

### 3. 除錯原則
- 在識別問題根本原因之前，不修改任何程式碼
- 一次只刪除或清理一個檔案或函數，避免混淆
- 使用詳細的錯誤訊息進行問題診斷

## 表單設計規範

### 欄位命名規則
每個欄位的第一行標籤格式：
```
中文名稱 (API_Name)
```

範例：
- 客戶姓名 (Customer_Name)
- 出生日期 (Birth_Date)
- 農曆生日 (Lunar_Birthday)

### 程式碼中的欄位存取
- 在所有 Deluge 程式碼中，**必須**使用括號中的 `API_Name` 來存取欄位值
- 正確用法：`input.Customer_Name`、`record.Birth_Date`
- 錯誤用法：`input.客戶姓名`、`record.出生日期`

## 曆法轉換規範

### 農曆轉換
- 使用現有的 `SolarToLunar` 函數進行陽曆轉農曆
- 不要重新編寫農曆轉換器
- 命名空間規範：
  - 二十八星宿（東方）：使用 `Star` 命名空間
  - 西方星座：使用 `Zodiac` 命名空間

## 程式碼品質標準

### 註解規範
- 使用行註解 `//` 而非區塊註解 `/* */`
- 提供詳細的繁體中文說明
- 包含函數用途、參數說明、回傳值說明

### 錯誤處理
- 採用「單一出口」模式
- 在函數開頭宣告結果變數
- 統一在函數末端回傳結果

### 語法限制遵守
- 避免過長的條件判斷
- 分步驟處理 Map 的連鎖調用
- 使用 `containKey` 而非 `containsKey`
- 查詢時提供適當的條件

## 檔案管理
- 按功能模組組織檔案
- 保持檔案結構清晰
- 定期清理不需要的檔案

## 協作與同步流程

### 核心觀念：開發環境 vs. Creator 環境
- **本地開發環境**: 用於程式碼撰寫、版本控制和結構化管理
- **Zoho Creator 環境**: 程式碼最終執行的地方
- **重要**: 本地開發的 `.deluge` 檔案不會自動同步到 Zoho Creator

### 同步步驟
1. **AI 完成開發**: 在本地環境完成函數開發
2. **AI 發出提醒**: 明確告知函數已完成，提醒手動同步
3. **使用者手動同步**:
   - 複製完整的函數內容
   - 登入 Zoho Creator，找到對應函數
   - 將程式碼貼上到 Creator 編輯器並儲存
4. **測試與回饋**: 在 Creator 環境中執行測試並回饋結果

### 重要提醒
- 手動同步步驟是合作成功的關鍵
- 每當一個功能單元開發完成後，都必須執行此流程
- 確保雙方看到的是相同版本的程式碼

## 常見陷阱避免

### 函數執行限制
- 避免直接執行以 map 為參數的獨立函數
- 建立不含參數的「主測試函數」來呼叫複雜函數
- 這可以避免編輯器底層錯誤

### 類型轉換注意事項
- API 回應的 ID 必須使用 `.toLong()` 轉換
- 避免使用不存在的 `.toBigInt()` 函數
- 客戶ID 查詢時必須轉換為 `Long` 型別

### 日期時間處理
- 使用 `.toDateTime()` 而非 `.toDate()` 來保留完整時間
- 使用 `addDay()` 而非 `addDays()` 進行日期加減
- 注意時辰計算的準確性

## 開發最佳實務

### 函數設計原則
1. **先設計函數簽名和參數**
2. **確認資料來源和查詢邏輯**
3. **實作核心計算邏輯**
4. **加入錯誤處理和驗證**
5. **撰寫清楚的註解和文件**

### 回傳值規範
```deluge
// 成功時的回傳格式
result.put("success", true);
result.put("data", actual_data);

// 錯誤時的回傳格式
result.put("error", "具體錯誤訊息");
result.put("success", false);
```

### 錯誤處理模式
```deluge
try
{
    // 主要邏輯
    result.put("success", true);
}
catch (e)
{
    result.put("error", "錯誤描述：" + e);
    result.put("success", false);
}
```
